import 'package:flutter/material.dart';

class AppColors {
  // Primary - A richer, more modern gold
  static const primary = Color(0xFFC9A334);   // Deeper Gold
  static const primaryLight = Color(0xFFF4E0A4); // Light Gold for subtle backgrounds
  
  // Secondary - A warmer, softer dark brown
  static const secondary = Color(0xFF4A2E20); // Warmer Dark Brown
  
  // Accent - A calm, modern blue
  static const accent = Color(0xFF6BAFCF);    // Calmer Modern Blue
  
  // Background - A very light, clean warm white
  static const background = Color(0xFFFCFAF5);// Cleaner Warm White
  static const surface = Color(0xFFFFFFFF);
  
  // Text - High contrast for readability
  static const textPrimary = Color(0xFF1E1E1E);
  static const textSecondary = Color(0xFF757575);
  static const textHint = Color(0xFFBDBDBD);
  
  // Feedback Colors
  static const error = Color(0xFFD32F2F);
  static const success = Color(0xFF388E3C);
  static const warning = Color(0xFFF57C00);
}